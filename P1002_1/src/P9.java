public class P9 {
    public static void main(String[] args) {
        
        // 정렬

        int[] arr = {7,8,9,1,3,4,5,6,2} ;

        // 제일 큰 수를 맨 뒤로 미는걸 반복해서 작은순대로 정렬
        // 아래는 미완성 코드

        for(int x = 0 ; x < arr.length ; x++ ) {
            for(int y = 0 ; y < arr.length ; y++) {
                if(arr[y] > arr[y+1]) {
                    
                }
            } 
        }
        
        for(int e : arr) {
            System.out.println(e);
        }

    }
}

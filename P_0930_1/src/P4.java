public class P4 {
    public static void main(String[] args) {
        
        // 연산자 - !!연산자는 타입, 특히 연산 결과의 타입에 굉장히 주의해야 한다!!

        // 산술 연산자: + - * / %
        // 비교 연산자: == != > < >= <=
        // 논리 연산자: && || !
        // 단축배정 연산자: += -= *= ... 다 된다
        // 증감 연산자: ++ --

        // 연산자는 우선순위가 정해져 있는데, 이건 그냥 자연스럽게 적응함.
    

    }
}
